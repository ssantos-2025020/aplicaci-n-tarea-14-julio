{
  "name": "backend",
  "version": "1.0.0",
  "description": "Backend TypeScript/Node.js - Módulo de persistencia de Clientes con fs/promises",
  "main": "dist/app.js",
  "packageManager": "pnpm@9.1.0",
  "engines": {
    "pnpm": ">=8"
  },
  "scripts": {
    "preinstall": "node -e \"if(!process.env.npm_execpath || !process.env.npm_execpath.includes('pnpm')) { console.error('Este proyecto solo permite instalar dependencias con pnpm. Usa: pnpm install'); process.exit(1); }\"",
    "build": "tsc",
    "start": "node dist/app.js",
    "dev": "ts-node-dev --respawn src/app.ts"
  },
  "dependencies": {
    "express": "^4.19.2",
    "cors": "^2.8.5"
  },
  "devDependencies": {
    "typescript": "^5.5.4",
    "@types/express": "^4.17.21",
    "@types/node": "^20.14.10",
    "@types/cors": "^2.8.17",
    "ts-node-dev": "^2.0.0"
  }
}
